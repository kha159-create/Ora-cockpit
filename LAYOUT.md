# تخطيط الصفحات — اتفاقيات موحّدة (هوية من cockpit)

الهوية البصرية (ألوان، أزرار، جداول، بطاقات) مأخوذة من مشروع **cockpit** ومطبّقة في `assets/sidebar.css`. كل الصفحات تستخدم نفس الخط (Cairo)، نفس البرتقالي (--primary-500/600)، ونفس الأزرار (.btn-primary, .btn-secondary) والجداول (.table-cockpit / .table-custom).

لتوحيد شكل الداشبورد وباقي الصفحات، استخدم الهيكل التالي في كل صفحة (تحت `main-content-inner`).

## 1. ترتيب العناصر

1. **عنوان الصفحة** — `page-header`
   - `page-title`: عنوان واضح (مثل: لوحة التحكم، أداء الموظفين)
   - `page-subtitle`: جملة قصيرة توضّح محتوى الصفحة

2. **الفلاتر** — `page-filters`
   - بطاقة واحدة تجمع كل الفلاتر (مدير المنطقة، الفرع، الفترة، إلخ)
   - نفس الشكل في كل الصفحات: خلفية بيضاء، ظل، حواف مستديرة

3. **أقسام المحتوى** — `page-section`
   - كل قسم رئيسي (KPIs، جدول، رسوم) داخل `<section class="page-section">`
   - بين الأقسام مسافة موحّدة (`--space-section`)

4. **تسميات الأقسام**
   - عنوان قسم: `section-title` (خط برتقالي على اليمين)
   - تسمية ثانوية (مثل "أداء العام الحالي"): `section-label`

## 2. الـ CSS (في `assets/sidebar.css`)

| Class | الاستخدام |
|-------|-----------|
| `page-header` | غلاف عنوان الصفحة |
| `page-title` | العنوان الرئيسي |
| `page-subtitle` | الوصف القصير |
| `page-filters` | بطاقة الفلاتر |
| `page-section` | قسم محتوى (مسافة أسفل موحّدة) |
| `section-title` | عنوان قسم (برتقالي) |
| `section-label` | تسمية ثانوية (رمادي) |
| `kpi-card` | بطاقة KPI (مستديرة، ظل) |
| `cockpit-section` | قسم أبيض مع ظل (موظفين، جداول) |

## 3. المسافات

- `--space-section`: بين الأقسام الكبيرة (1.5rem)
- `--space-card`: بين البطاقات داخل القسم (1rem)
- `--space-inline`: بين عناصر صغيرة (0.75rem)

## 4. الصفحات المطبّق عليها

- **index.html** — لوحة التحكم: عنوان، فلاتر، أقسام (KPIs الحالي، السابق، التقرير اليومي، رسوم، تحليل متقدم، جدول التفاصيل)
- **employees.html** — أداء الموظفين: عنوان، فلاتر، أقسام (KPIs، Charts، Leaderboard)
- **branch_details.html** — تفاصيل الفرع: عنوان، أقسام (مبيعات الأمس/الشهر)
- **product_analysis.html** — تحليل المنتجات: عنوان، فلاتر، أقسام (ملخص الفئات والمنتجات)
- **reports.html** — التقارير: عنوان، شريط أزرار، أقسام (روابط سريعة)
- **stagnant_products.html** — المنتجات الراكدة: عنوان، فلاتر، أقسام (إحصائيات، جدول)
- **offers_analysis.html** — تحليل العروض: عنوان، فلاتر، أقسام (إحصائيات، عروض، جدول)
- **data_audit.html** — تدقيق البيانات: عنوان، فلاتر، أقسام (بطاقات التدقيق)
- **target_setting.html** — تحديد التارجت: عنوان، فلاتر، أقسام (جدول التارجت)
- **admin_targets.html** — إعدادات الأهداف: عنوان (ثيم داكن)، محتوى
- **chatbot.html** — الشات بوت: عنوان، قسم (حاوية المحادثة)
